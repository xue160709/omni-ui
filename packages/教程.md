# OmniUI 接入教程

这份教程面向**应用开发者**，说明如何把 OmniUI 接入已有的 React 项目。

## 先选包

普通 React 项目只装一个包：

```bash
npm install @omni-ui/react
```

不需要手动安装 `@omni-ui/core`，也不需要安装 `@omni-ui/shadcn`。

详细说明：

- `@omni-ui/react` 已经依赖 `@omni-ui/core`，并且会重新导出常用的 core API。
- `@omni-ui/shadcn` 只是可选的 shadcn registry / starter recipe，不是 runtime 必需品。
- 你的业务 action、页面对象、按钮点击逻辑都写在自己的应用里，不需要三个包全装一遍。

## 三个包的关系

| 包 | 什么时候用 | 是否需要手动安装 |
| --- | --- | --- |
| `@omni-ui/react` | React 应用接入多模态能力。Provider、页面/对象标记、route/action 注册、命令输入、assistant 都在这里。 | 大多数项目只装这个 |
| `@omni-ui/core` | 非 React adapter、服务端 resolver、测试环境、框架无关类型和校验能力。 | React 项目通常不用手动装 |
| `@omni-ui/shadcn` | 你想用 shadcn CLI 安装可编辑的 `components/multimodal/*` wrapper 或 starter recipe。 | 可选，需要 shadcn 风格源码时再用 |

换句话说：

- 已有 React 项目：装 `@omni-ui/react`。
- 新 React 项目，但还没决定 UI 库：还是先装 `@omni-ui/react`。
- 已经在用 shadcn/ui，并且想要现成的 Button、Input、Dialog、DataTable、AssistantPanel wrapper：再加 shadcn registry item。
- 要给 Vue、Svelte、Web Components 或服务端流程做底层集成：才直接用 `@omni-ui/core`。

## 快速问答

**`@omni-ui/react` 需要安装 `@omni-ui/core` 吗？**

不需要。`@omni-ui/react` 的 `dependencies` 里已经包含 `@omni-ui/core`。在 React 应用里也优先从 `@omni-ui/react` import，它会重新导出常用类型和工具。

**`@omni-ui/react` 需要安装 `@omni-ui/shadcn` 吗？**

不需要。shadcn 是可选源码配方。只有当你想用 shadcn CLI 生成 `components/multimodal/*` 这些可编辑 wrapper 时，才使用 registry item。

**三个包需要都安装吗？**

通常不需要。React 应用默认只安装 `@omni-ui/react`。你明确要写非 React adapter 或服务端 resolver 时再直接安装 `@omni-ui/core`；你明确要 shadcn wrapper 时再使用 shadcn registry。

## 安装方式

### 路径 A：普通 React 项目

```bash
npm install @omni-ui/react
```

然后从 `@omni-ui/react` 引入所有 React runtime API：

```tsx
import {
  MultimodalProvider,
  MultimodalPage,
  MultimodalGroup,
  defineMultimodalConfig,
  useInteractionActions,
  useInteractionApi,
} from "@omni-ui/react"
```

### 路径 B：React + shadcn/ui 项目

先按普通 React 项目接入 runtime。然后按需安装 registry item：

```bash
npm install @omni-ui/react
npx shadcn@latest add <registry-url>/r/multimodal-provider.json
npx shadcn@latest add <registry-url>/r/multimodal-button.json
```

在本仓库本地开发 registry 时，先启动 docs，再用 localhost URL：

```bash
npm run dev
npx shadcn@latest add http://127.0.0.1:5173/r/multimodal-provider.json
npx shadcn@latest add http://127.0.0.1:5173/r/multimodal-button.json
```

registry item 会把可编辑源码安装到你的 `components/multimodal/*`。这些文件底层仍然 import `@omni-ui/react`，不会替换或覆盖你的 `components/ui/*`。

### 路径 C：非 React 或服务端能力

```bash
npm install @omni-ui/core
```

只在你需要框架无关能力时直接使用它，例如：

- 你在 React 之外实现自己的 runtime adapter，例如 Vue、Svelte 或原生 Web Components。
- 你要在服务端或测试环境里复用 resolver、manifest、assistant prompt、action validation。
- 你要写自定义 `IntentResolver`，并让它和 React runtime 的本地校验、执行链路配合。

## 心智模型

OmniUI 的 runtime 会维护两层上下文：

- `Interaction Snapshot`：当前页面运行时快照。包含当前页面、可见对象、对象状态、可执行 action、焦点、弹窗上下文。
- `App Manifest`：全局能力索引。包含已注册的路由、全局命令、跨页面能力。它不是整个项目源码，也不是全量业务数据。

用户说一句话时，链路是：

```text
utterance
  -> configured local rules / built-in rule resolver
  -> optional LLM fallback
  -> local validation
  -> registered action executor
```

LLM 只能提出候选 action，不能绕过本地校验直接改状态。

接入路径概览：

```text
@omni-ui/react
  -> MultimodalProvider
  -> MultimodalPage / MultimodalGroup / useInteractionNode
  -> useInteractionRoutes / useInteractionActions
  -> useInteractionApi / useAssistantConversation
```

---

## 第 1 步：创建配置

建议在应用里放一个 `multimodal.config.ts`。

```ts
import {
  NAVIGATION_GOTO_ACTION_ID,
  defineMultimodalConfig,
} from "@omni-ui/react"

export const multimodalConfig = defineMultimodalConfig({
  rules: [
    {
      id: "navigation.goto",
      patterns: ["打开{route}", "去{route}", "进入{route}", "回到{route}"],
      target: "route.byLabel",
      actionId: NAVIGATION_GOTO_ACTION_ID,
    },
    {
      id: "issue.close",
      patterns: ["关闭{issue}", "把{issue}关闭", "完成{issue}"],
      target: "entity.issue.byLabelOrIndex",
      actionId: "issue.close",
    },
  ],
  llm: {
    localFastPath: {
      mode: "allowlist",
      actionIds: ["navigation.*"],
      allowPrimitiveActions: false,
    },
    modelActionPolicy: {
      mode: "allowlist",
      actionIds: ["navigation.*", "issue.*"],
      allowPrimitiveActions: false,
      requireConfirmationForRisk: ["medium", "high"],
    },
  },
})
```

`navigation.goto` 是库提供的通用 action。`issue.close` 是你的应用自己的 action，后面需要用 `useInteractionActions()` 注册执行逻辑。

常用 target：

- `route.byLabel`：按路由 label/alias 找目标。
- `object.byLabel`：按可见对象 label/alias 找目标。
- `object.byIndex`：按"第一个/第二个"找可见对象。
- `object.byLabelOrIndex`：先按序号，找不到再按名称。
- `entity.issue.byLabelOrIndex`：只在 `entity.type === "issue"` 的对象里找。
- `page.current`：当前页面作为目标。

## 第 2 步：包裹应用

在应用根部加 `MultimodalProvider`。

```tsx
import { MultimodalProvider } from "@omni-ui/react"
import { multimodalConfig } from "./multimodal.config"

export function App() {
  return (
    <MultimodalProvider config={multimodalConfig}>
      <AppRuntime />
    </MultimodalProvider>
  )
}
```

如果你有自己的 LLM resolver，也可以传给 Provider：

```tsx
<MultimodalProvider
  config={multimodalConfig}
  resolvers={[myLlmResolver]}
  resolverMode="rule-first"
>
  <AppRuntime />
</MultimodalProvider>
```

默认推荐 `rule-first`：先本地解析，置信度不够再让 LLM 兜底。

## 第 3 步：注册全局路由

跨页面能力不要让开发者手写全局地图。用 `useInteractionRoutes()` 在 app root 注册路由即可，它会自动：

- 创建可被本地规则/LLM 找到的 route object。
- 注册内置 `navigation.goto` action。
- 把路由写进 App Manifest，给 LLM 作为全局能力上下文。

```tsx
import { useInteractionRoutes } from "@omni-ui/react"

type AppRoute =
  | { screen: "home" }
  | { screen: "issues" }
  | { screen: "settings" }

function AppRuntime() {
  const [route, setRoute] = useAppRoute()

  useInteractionRoutes<AppRoute>({
    routes: [
      {
        id: "app.route.home",
        label: "首页",
        aliases: ["主页"],
        path: "/",
        route: { screen: "home" },
        active: route.screen === "home",
      },
      {
        id: "app.route.issues",
        label: "问题列表",
        aliases: ["问题", "工单"],
        path: "/issues",
        route: { screen: "issues" },
        active: route.screen === "issues",
      },
      {
        id: "app.route.settings",
        label: "设置",
        aliases: ["配置"],
        path: "/settings",
        route: { screen: "settings" },
        active: route.screen === "settings",
      },
    ],
    execute: (nextRoute) => setRoute(nextRoute),
  })

  return <CurrentScreen route={route} />
}
```

现在用户说"打开设置"，即使设置页面还没渲染，runtime 也能通过 App Manifest 找到路由并执行 `navigation.goto`。

## 第 4 步：标记页面和业务对象

页面用 `MultimodalPage`。

```tsx
import { MultimodalPage } from "@omni-ui/react"

function IssuesPage({ issues }: { issues: Issue[] }) {
  return (
    <MultimodalPage
      id="page.issues"
      title="问题列表"
      route="/issues"
      state={{
        totalCount: issues.length,
        openCount: issues.filter((issue) => issue.status === "open").length,
      }}
    >
      <IssueList issues={issues} />
    </MultimodalPage>
  )
}
```

业务列表和列表项用 `MultimodalGroup`。重点是给业务对象挂 `entity`。

```tsx
import { MultimodalGroup } from "@omni-ui/react"

function IssueList({ issues }: { issues: Issue[] }) {
  return (
    <MultimodalGroup id="issue.list" role="list" label="问题列表" indexBy="visible_order">
      {issues.map((issue) => (
        <MultimodalGroup
          key={issue.id}
          id={`issue.item.${issue.id}`}
          role="list_item"
          label={issue.title}
          aliases={[`#${issue.number}`]}
          entity={{ type: "issue", id: issue.id }}
          state={{
            status: issue.status,
            priority: issue.priority,
          }}
        >
          <IssueRow issue={issue} />
        </MultimodalGroup>
      ))}
    </MultimodalGroup>
  )
}
```

这样用户说"关闭第一个"或"关闭登录失败那个"，本地规则可以解析到对应 `issue.item.*`。

## 第 5 步：注册业务 Action

业务 action 由应用自己定义。GUI 点击和多模态执行应该共用同一个 executor。

```tsx
import * as React from "react"
import {
  useInteractionActions,
  type ActionContext,
  type ActionPayload,
} from "@omni-ui/react"

type IssueAction =
  | { type: "issue.close"; issueId: string }
  | { type: "issue.reopen"; issueId: string }
  | { type: "issue.assign"; issueId: string; assigneeId: string }

function IssuesRuntime({ closeIssue, reopenIssue, assignIssue }: Props) {
  const executeIssueAction = React.useCallback((action: ActionPayload) => {
    const issueAction = action as IssueAction

    if (issueAction.type === "issue.close") {
      closeIssue(issueAction.issueId)
      return
    }

    if (issueAction.type === "issue.reopen") {
      reopenIssue(issueAction.issueId)
      return
    }

    if (issueAction.type === "issue.assign") {
      assignIssue(issueAction.issueId, issueAction.assigneeId)
    }
  }, [assignIssue, closeIssue, reopenIssue])

  useInteractionActions({
    namespace: "issue",
    actions: {
      "issue.close": {
        attachTo: { entityType: "issue" },
        executeScope: "object",
        paramsFrom: ({ target }: ActionContext) => ({
          issueId: target.entity?.id,
        }),
        availableWhen: ({ target }: ActionContext) => target.state?.status === "open",
        risk: "medium",
      },
      "issue.reopen": {
        attachTo: { entityType: "issue" },
        executeScope: "object",
        paramsFrom: ({ target }: ActionContext) => ({
          issueId: target.entity?.id,
        }),
        availableWhen: ({ target }: ActionContext) => target.state?.status === "closed",
      },
      "issue.assign": {
        attachTo: { entityType: "issue" },
        executeScope: "object",
        paramsFrom: ({ target, candidate }: ActionContext) => ({
          issueId: target.entity?.id,
          assigneeId: candidate?.params?.assigneeId,
        }),
      },
    },
    execute: executeIssueAction,
  })

  return null
}
```

`risk: "medium"` 或 `requiresConfirmation: true` 会被本地校验和 assistant policy 使用。LLM 返回的 action 也必须经过这些校验。

## 第 6 步：添加文本或语音入口

如果你只想要一个低层输入框，可以用 `useInteractionApi()`。

```tsx
import * as React from "react"
import { useInteractionApi } from "@omni-ui/react"

function CommandInput() {
  const interaction = useInteractionApi()
  const [text, setText] = React.useState("")

  async function submit() {
    const result = await interaction.submitUtterance(text)

    if (!result.ok) {
      console.warn(result.validation?.code, result.error)
    }
  }

  return (
    <form
      onSubmit={(event) => {
        event.preventDefault()
        void submit()
      }}
    >
      <input value={text} onChange={(event) => setText(event.target.value)} />
      <button type="submit">执行</button>
    </form>
  )
}
```

常用 API：

- `getSnapshot()`：读取当前 Interaction Snapshot。
- `resolveText(text)`：只解析，不执行。
- `dispatchResolution(resolution)`：执行一个已解析的候选 action。
- `submitUtterance(text)`：解析、校验、执行一条用户输入。

`submitUtterance()` 会返回结构化结果。常见失败类型：

| `status` / `code` | 含义 | 建议处理 |
| --- | --- | --- |
| `resolution.status === "not_found"` | 没有解析出目标或 action | 提示用户换个说法 |
| `target_missing` | 解析到了目标 id，但执行时目标已不在当前 snapshot | 重新打开页面或重新解析 |
| `missing_action` | 目标存在，但没有注册对应 action | 检查是否漏了 `useInteractionActions()` |
| `unavailable` | action 当前不可用（例如已关闭的问题不能再次关闭） | 提示当前状态不允许该操作 |
| `confirmation_required` | 高风险操作需要用户确认 | 用 `confirmedActionId` 重新提交 |
| `state_changed` | 解析时和执行时页面状态不同 | 重新解析用户输入 |
| `policy_denied` | LLM 返回的 action 不符合本地 allowlist 或安全策略 | 提示操作不被允许 |

产品里通常把这些错误转成 toast、assistant 回复或表单提示；不要只依赖 `console.warn()`。

## 第 7 步：添加带 LLM 兜底的助手

聊天助手推荐用 `useAssistantConversation()`。它只要求你提供一个 `callModel(messages)`，不绑定 OpenAI、Anthropic 或任何特定 provider。

```tsx
import * as React from "react"
import { useAssistantConversation } from "@omni-ui/react"

function AssistantInput() {
  const conversation = useAssistantConversation({
    assistantOptions: {
      localFastPath: {
        mode: "allowlist",
        actionIds: ["navigation.*"],
        allowPrimitiveActions: false,
      },
      modelActionPolicy: {
        mode: "allowlist",
        actionIds: ["navigation.*", "issue.*"],
        allowPrimitiveActions: false,
        requireConfirmationForRisk: ["medium", "high"],
      },
      localReply: {
        actionReplies: {
          "navigation.goto": ({ result }) => `已打开${result.target?.label ?? "目标"}。`,
          "issue.close": ({ targetLabel }) => `已关闭${targetLabel}。`,
        },
      },
    },
    callModel: async (messages) => {
      const response = await fetch("/api/chat", {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ messages }),
      })
      const data = await response.json()
      return data.content
    },
  })

  return (
    <form onSubmit={(event) => {
      event.preventDefault()
      void conversation.submitMessage()
    }}>
      {conversation.messages.map((message) => (
        <p key={message.id}>{message.content}</p>
      ))}
      {conversation.pendingModelAction ? (
        <button type="button" onClick={() => void conversation.confirmPendingModelAction()}>
          确认执行
        </button>
      ) : null}
      <input
        value={conversation.draft}
        onChange={(event) => conversation.setDraft(event.target.value)}
      />
      <button disabled={conversation.status === "sending"}>发送</button>
    </form>
  )
}
```

`callModel(messages)` 收到的 messages 已经包含 system prompt，其中会包含：

- 当前 compact Interaction Snapshot。
- compact App Manifest。
- 操作输出格式说明。

它不会包含整个项目源码、未注册页面、未暴露的业务数据。

### 服务端适配示例

OpenAI-compatible 服务端：

```ts
const upstream = await fetch(`${baseUrl}/chat/completions`, {
  method: "POST",
  headers: {
    "content-type": "application/json",
    authorization: `Bearer ${apiKey}`,
  },
  body: JSON.stringify({ model, messages: body.messages }),
})
const data = await upstream.json()
return data.choices?.[0]?.message?.content ?? ""
```

Anthropic 服务端（需要把 system message 拆出来）：

```ts
const [system, ...rest] = body.messages
const upstream = await fetch("https://api.anthropic.com/v1/messages", {
  method: "POST",
  headers: {
    "content-type": "application/json",
    "x-api-key": apiKey,
    "anthropic-version": "2023-06-01",
  },
  body: JSON.stringify({
    model,
    max_tokens: 1024,
    system: system?.role === "system" ? system.content : undefined,
    messages: rest
      .filter((message) => message.role !== "system")
      .map((message) => ({
        role: message.role === "assistant" ? "assistant" : "user",
        content: message.content,
      })),
  }),
})
const data = await upstream.json()
return data.content?.map((part) => part.text ?? "").join("") ?? ""
```

## 第 8 步（可选）：服务端 LLM Resolver

如果你想把"意图解析"也交给服务端 LLM，可以实现一个 resolver。

```ts
// Server-only code. Do not bundle this file into the browser.
import { createOpenAIResolver } from "@omni-ui/core"

export const resolver = createOpenAIResolver({
  model: process.env.OPENAI_MODEL,
  apiKeyEnv: "OPENAI_API_KEY",
})
```

浏览器里不要直接放 API key。推荐做一个自己的 server endpoint，让前端只发送 `messages` 或 `utterance + snapshot`。

无论 LLM 在哪里运行，它返回的 action 都会回到本地 runtime 做校验和 dispatch。

## 第 9 步（可选）：shadcn Registry

如果你装了 shadcn registry，可以用 `components/multimodal/*` 里的组件替换部分基础 UI 组件，也可以安装更高层的 starter recipe。

```tsx
import { MultimodalButton } from "@/components/multimodal/button"
import { MultimodalInput } from "@/components/multimodal/input"

<MultimodalInput
  interactionId="issue.search"
  interactionLabel="搜索问题"
  placeholder="搜索问题"
/>

<MultimodalButton
  interactionId="issue.create"
  interactionLabel="新建问题"
>
  新建
</MultimodalButton>
```

基础 wrapper 适合按钮、输入框、选项卡这类控件。业务对象仍建议用 `MultimodalGroup` 表达边界和 `entity`。

更高层 recipe 适合新项目起步，例如 assistant panel、form、data table。它们不是黑盒组件库；安装后就是你项目里的源码，样式和结构都可以按你的设计系统修改。

```tsx
import { MultimodalAssistantPanel } from "@/components/multimodal/assistant-panel"
import { MultimodalDataTable } from "@/components/multimodal/data-table"
```

---

## 你和库的分工

**你需要负责：**

- 定义业务 action id，例如 `issue.close`、`order.cancel`。
- 注册 action 的 `attachTo`、`executeScope`、`paramsFrom`、`availableWhen`。
- 让 GUI 点击和多模态执行共用同一个业务 executor。
- 给关键业务对象提供稳定的 `id`、`label`、`entity` 和必要 `state`。
- 在 app root 注册 routes，让跨页面能力进入 manifest。

**库负责：**

- 抽取 DOM/ARIA 和 registered groups。
- 生成 Interaction Snapshot。
- 合并 App Manifest。
- 提供内置 `navigation.goto`。
- 执行本地规则和 resolver 链路。
- 校验 action 是否存在、目标是否还在、状态是否变化、是否需要确认。
- 把 action 分发到你注册的 executor。

## 接入检查清单

1. 在根节点加 `MultimodalProvider config={multimodalConfig}`。
2. 在 app root 用 `useInteractionRoutes()` 注册全局路由。
3. 每个页面加 `MultimodalPage`。
4. 业务列表和详情对象加 `MultimodalGroup`，并设置 `entity`。
5. 用 `useInteractionActions()` 注册业务 action。
6. 在 `multimodal.config.ts` 写本地规则。
7. 用 `useInteractionApi()` 或 `useAssistantConversation()` 接入输入框、语音或聊天。
8. 为高风险 action 设置 `risk` 或 `requiresConfirmation`。

## 常见坑

- 不要把业务 action 放进库里。库只提供通用能力；业务 action 属于 app。
- 不要让 LLM 直接改业务状态。LLM 只能返回候选 action。
- 不要依赖未加载页面的 runtime state。跨页面只通过 route/global manifest 导航过去，再读取新页面 snapshot。
- 不要给每个按钮手写语音命令。优先给业务对象声明 `label`、`entity`、`state`，再集中注册 action。
- 不要把 API key 放浏览器里。LLM fallback 应该通过你自己的服务端。

## 常用导出速查

React 接入常用：

- `MultimodalProvider`
- `MultimodalPage`
- `MultimodalGroup`
- `useInteractionNode`
- `useInteractionRoutes`
- `useInteractionNavigationHistory`
- `useInteractionActions`
- `useInteractionApi`
- `useAssistantConversation`
- `useInteractionSnapshot`

配置、resolver 和类型：

- `defineMultimodalConfig`
- `defineInteractionManifest`
- `createConfiguredRuleResolver`
- `createOpenAIResolver`
- `createAnthropicResolver`
- `NAVIGATION_GOTO_ACTION_ID`
- `NAVIGATION_BACK_ACTION_ID`
- `NAVIGATION_FORWARD_ACTION_ID`
- `ActionContext`
- `ActionPayload`
- `InteractionObject`
- `InteractionSnapshot`
- `IntentResolver`
- `ResolvedInteraction`
- `InteractionSubmitResult`
